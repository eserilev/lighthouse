-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [slashing_protection]
import Aeneas
open Aeneas Aeneas.Std Result ControlFlow Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false
set_option linter.style.whitespace false
set_option linter.style.setOption false
set_option linter.style.longLine false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

/- You can set the `maxRecDepth` value with the `-max-recdepth` CLI option -/
set_option maxRecDepth 2048

namespace slashing_protection

/-- [slashing_protection::pure_check::AttRow]
    Source: 'validator_client/slashing_protection/src/pure_check.rs', lines 28:0-32:1
    Visibility: public -/
structure pure_check.AttRow where
  source : Std.U64
  target : Std.U64
  root : Array Std.U8 32#usize

/-- [slashing_protection::pure_check::Verdict]
    Source: 'validator_client/slashing_protection/src/pure_check.rs', lines 36:0-45:1
    Visibility: public -/
@[discriminant isize]
inductive pure_check.Verdict where
| Valid : pure_check.Verdict
| SameData : pure_check.Verdict
| SourceExceedsTarget : pure_check.Verdict
| DoubleVote : pure_check.Verdict
| PrevSurroundsNew : pure_check.Verdict
| NewSurroundsPrev : pure_check.Verdict
| SourceLessThanLowerBound : pure_check.Verdict
| TargetLessThanOrEqLowerBound : pure_check.Verdict

/-- [slashing_protection::pure_check::roots_eq]: loop body 0:
    Source: 'validator_client/slashing_protection/src/pure_check.rs', lines 59:4-67:5 -/
@[rust_loop_body]
def pure_check.roots_eq_loop.body
  (a : Array Std.U8 32#usize) (b : Array Std.U8 32#usize) (a_is_null : Bool)
  (equal : Bool) (i : Std.Usize) :
  Result (ControlFlow (Bool × Bool × Std.Usize) (Bool × Bool))
  := do
  if i < 32#usize
  then
    let i1 ← Array.index_usize a i
    let a_is_null1 ← if i1 != 0#u8
                       then ok false
                       else ok a_is_null
    let i2 ← Array.index_usize b i
    let equal1 ← if i1 != i2
                   then ok false
                   else ok equal
    let i3 ← i + 1#usize
    ok (cont (a_is_null1, equal1, i3))
  else ok (done (a_is_null, equal))

/-- [slashing_protection::pure_check::roots_eq]: loop 0:
    Source: 'validator_client/slashing_protection/src/pure_check.rs', lines 59:4-67:5 -/
@[rust_loop]
def pure_check.roots_eq_loop
  (a : Array Std.U8 32#usize) (b : Array Std.U8 32#usize) (a_is_null : Bool)
  (equal : Bool) (i : Std.Usize) :
  Result (Bool × Bool)
  := do
  loop
    (fun (a_is_null1, equal1, i1) => pure_check.roots_eq_loop.body a b
      a_is_null1 equal1 i1)
    (a_is_null, equal, i)

/-- [slashing_protection::pure_check::roots_eq]:
    Source: 'validator_client/slashing_protection/src/pure_check.rs', lines 55:0-69:1 -/
def pure_check.roots_eq
  (a : Array Std.U8 32#usize) (b : Array Std.U8 32#usize) : Result Bool := do
  let (a_is_null, equal) ← pure_check.roots_eq_loop a b true true 0#usize
  if equal
  then ok (¬ a_is_null)
  else ok false

/-- [slashing_protection::pure_check::check_attestation_pure]: loop body 0:
    Source: 'validator_client/slashing_protection/src/pure_check.rs', lines 86:4-94:5
    Visibility: public -/
@[rust_loop_body]
def pure_check.check_attestation_pure_loop0.body
  (history : Slice pure_check.AttRow) (i : Std.U64) (a : Array Std.U8 32#usize)
  (n : Std.Usize) (same_target_found : Bool) (same_root : Bool)
  (i1 : Std.Usize) :
  Result (ControlFlow (Bool × Bool × Std.Usize) (Bool × Bool))
  := do
  if i1 < n
  then
    let ar ← Slice.index_usize history i1
    let (same_target_found1, same_root1) ←
      if ar.target = i
      then
        do
        let b ← pure_check.roots_eq ar.root a
        let b1 ← if b
                   then ok true
                   else ok same_root
        ok (true, b1)
      else ok (same_target_found, same_root)
    let i2 ← i1 + 1#usize
    ok (cont (same_target_found1, same_root1, i2))
  else ok (done (same_target_found, same_root))

/-- [slashing_protection::pure_check::check_attestation_pure]: loop 0:
    Source: 'validator_client/slashing_protection/src/pure_check.rs', lines 86:4-94:5
    Visibility: public -/
@[rust_loop]
def pure_check.check_attestation_pure_loop0
  (history : Slice pure_check.AttRow) (i : Std.U64) (a : Array Std.U8 32#usize)
  (n : Std.Usize) (same_target_found : Bool) (same_root : Bool)
  (i1 : Std.Usize) :
  Result (Bool × Bool)
  := do
  loop
    (fun (same_target_found1, same_root1, i2) =>
      pure_check.check_attestation_pure_loop0.body history i a n
      same_target_found1 same_root1 i2)
    (same_target_found, same_root, i1)

/-- [slashing_protection::pure_check::check_attestation_pure]: loop body 1:
    Source: 'validator_client/slashing_protection/src/pure_check.rs', lines 107:4-115:5
    Visibility: public -/
@[rust_loop_body]
def pure_check.check_attestation_pure_loop1.body
  (history : Slice pure_check.AttRow) (i : Std.U64) (i1 : Std.U64)
  (n : Std.Usize) (prev_surrounds : Bool) (new_surrounds : Bool)
  (i2 : Std.Usize) :
  Result (ControlFlow (Bool × Bool × Std.Usize) (Bool × Bool))
  := do
  if i2 < n
  then
    let ar ← Slice.index_usize history i2
    let prev_surrounds1 ←
      if ar.source < i
      then if ar.target > i1
           then ok true
           else ok prev_surrounds
      else ok prev_surrounds
    let new_surrounds1 ←
      if ar.source > i
      then if ar.target < i1
           then ok true
           else ok new_surrounds
      else ok new_surrounds
    let i3 ← i2 + 1#usize
    ok (cont (prev_surrounds1, new_surrounds1, i3))
  else ok (done (prev_surrounds, new_surrounds))

/-- [slashing_protection::pure_check::check_attestation_pure]: loop 1:
    Source: 'validator_client/slashing_protection/src/pure_check.rs', lines 107:4-115:5
    Visibility: public -/
@[rust_loop]
def pure_check.check_attestation_pure_loop1
  (history : Slice pure_check.AttRow) (i : Std.U64) (i1 : Std.U64)
  (n : Std.Usize) (prev_surrounds : Bool) (new_surrounds : Bool)
  (i2 : Std.Usize) :
  Result (Bool × Bool)
  := do
  loop
    (fun (prev_surrounds1, new_surrounds1, i3) =>
      pure_check.check_attestation_pure_loop1.body history i i1 n
      prev_surrounds1 new_surrounds1 i3)
    (prev_surrounds, new_surrounds, i2)

/-- [slashing_protection::pure_check::check_attestation_pure]: loop body 2:
    Source: 'validator_client/slashing_protection/src/pure_check.rs', lines 130:8-138:9
    Visibility: public -/
@[rust_loop_body]
def pure_check.check_attestation_pure_loop2.body
  (history : Slice pure_check.AttRow) (n : Std.Usize) (min_source : Std.U64)
  (min_target : Std.U64) (i : Std.Usize) :
  Result (ControlFlow (Std.U64 × Std.U64 × Std.Usize) (Std.U64 × Std.U64))
  := do
  if i < n
  then
    let ar ← Slice.index_usize history i
    let min_source1 ←
      if ar.source < min_source
      then ok ar.source
      else ok min_source
    let min_target1 ←
      if ar.target < min_target
      then ok ar.target
      else ok min_target
    let i1 ← i + 1#usize
    ok (cont (min_source1, min_target1, i1))
  else ok (done (min_source, min_target))

/-- [slashing_protection::pure_check::check_attestation_pure]: loop 2:
    Source: 'validator_client/slashing_protection/src/pure_check.rs', lines 130:8-138:9
    Visibility: public -/
@[rust_loop]
def pure_check.check_attestation_pure_loop2
  (history : Slice pure_check.AttRow) (n : Std.Usize) (min_source : Std.U64)
  (min_target : Std.U64) (i : Std.Usize) :
  Result (Std.U64 × Std.U64)
  := do
  loop
    (fun (min_source1, min_target1, i1) =>
      pure_check.check_attestation_pure_loop2.body history n min_source1
      min_target1 i1)
    (min_source, min_target, i)

/-- [slashing_protection::pure_check::check_attestation_pure]:
    Source: 'validator_client/slashing_protection/src/pure_check.rs', lines 74:0-148:1
    Visibility: public -/
def pure_check.check_attestation_pure
  (history : Slice pure_check.AttRow) (candidate : pure_check.AttRow) :
  Result pure_check.Verdict
  := do
  if candidate.source > candidate.target
  then ok pure_check.Verdict.SourceExceedsTarget
  else
    let n := Slice.len history
    let (same_target_found, same_root) ←
      pure_check.check_attestation_pure_loop0 history candidate.target
        candidate.root n false false 0#usize
    if same_target_found
    then
      if same_root
      then ok pure_check.Verdict.SameData
      else ok pure_check.Verdict.DoubleVote
    else
      let (prev_surrounds, new_surrounds) ←
        pure_check.check_attestation_pure_loop1 history candidate.source
          candidate.target n false false 0#usize
      if prev_surrounds
      then ok pure_check.Verdict.PrevSurroundsNew
      else
        if new_surrounds
        then ok pure_check.Verdict.NewSurroundsPrev
        else
          if n > 0#usize
          then
            let ar ← Slice.index_usize history 0#usize
            let (min_source, min_target) ←
              pure_check.check_attestation_pure_loop2 history n ar.source
                ar.target 1#usize
            if candidate.source < min_source
            then ok pure_check.Verdict.SourceLessThanLowerBound
            else
              if candidate.target <= min_target
              then ok pure_check.Verdict.TargetLessThanOrEqLowerBound
              else ok pure_check.Verdict.Valid
          else ok pure_check.Verdict.Valid

end slashing_protection
